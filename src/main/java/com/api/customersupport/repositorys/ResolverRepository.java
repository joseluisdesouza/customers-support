package com.api.customersupport.repositorys;

import com.api.customersupport.models.ResolverModel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ResolverRepository extends JpaRepository<ResolverModel, Long> {
}
